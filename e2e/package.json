{
  "name": "@push-to/node-e2e-scratch",
  "private": true,
  "type": "module",
  "description": "T-N1-E2E — live-stack integration for @push-to/node against a local PushTo core stack. NOT part of the published package (packages/node's package.json \"files\" is [\"dist\"] only; this directory sits outside packages/ entirely).",
  "scripts": {
    "test": "bun run live-integration.ts"
  },
  "dependencies": {
    "@negrel/http-ece": "npm:@jsr/negrel__http-ece@0.7.0",
    "@unkey/api": "2.3.5"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14"
  }
}
